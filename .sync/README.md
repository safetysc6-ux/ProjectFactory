# Sync triggers

The `sync/affiliate-d1` branch is used only to trigger the `affiliate-d1-sync` workflow by updating `.sync/affiliate-d1-trigger.txt`. Do not merge routine trigger commits into `main`.
